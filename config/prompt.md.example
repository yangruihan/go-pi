# 复制到 ~/.gopi/prompt.md
# 支持占位符：
#   {{BASE_PROMPT}}  -> gopi 内置系统提示
#   {{AGENT_MD}}     -> 项目根目录 AGENT.md 内容（若存在）

{{BASE_PROMPT}}

---

附加规则：
1. 输出优先给可执行步骤和结论。
2. 涉及改文件时先列将要改动的文件路径。
3. 复杂变更默认先给最小可运行版本。

项目规则：
{{AGENT_MD}}
